/*
Does random things that have to do with building certain types of graphs
*/

function GraphBuilder(){

}


GraphBuilder.prototype.generateAllConnectedNonIsomorphicGraphs = function(maxP){
	// maxP is the number of nodes to generate up to 
	
	
	
	var curP = 0;
	var curQ = 0;
	
	var nonIsomorphicGraphs = [];
	
	while(curP <= maxP){
		
		curP++;
	}
}

GraphBuilder.prototype.generateConnectedNonIsomorphicGraphsOfPQ = function(p,q){ 
	/*
	Generates all connected non-isomorphic graphs with vertices p and edges q stores them in an array and returns it
	*/
	
	
}